<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>
            <%= title %>
        </title>
        <link rel="icon" href="/favicon.svg" type="image/svg+xml">
        <link rel="stylesheet" href="/styles.css">
    </head>

    <body>
        <div class="container">
            <div class="action-bar">
                <h1>
                    <%= category.name %> Instruments
                </h1>
                <div class="action-buttons">
                    <a href="/instruments/new/<%= category.id %>">➕ Add Instrument</a>
                    <a href="/categories/edit/<%= category.id %>">✏️ Edit Category</a>
                    <a href="/categories/delete/<%= category.id %>" onclick="return confirm('Delete this category?')"
                        style="background: #ff4444; color: white;">🗑️ Delete Category</a>
                    <a href="/categories">← Back</a>
                </div>
            </div>

            <% if (instruments.length> 0) { %>
                <ul>
                    <% instruments.forEach(instrument=> { %>
                        <li>
                            <div class="instrument-info">
                                <strong style="font-size: 1.2rem; color: var(--gold-light);">
                                    <%= instrument.name %>
                                </strong>
                                <div style="margin-top: 8px; color: var(--gold-dark);">
                                    Brand: <%= instrument.brand %> | Quantity: <%= instrument.quantity %> | Price: $<%=
                                                instrument.price %>
                                </div>
                            </div>
                            <div class="instrument-actions">
                                <a href="/instruments/edit/<%= instrument.id %>">✏️ Edit</a>
                                <a href="/instruments/delete/<%= instrument.id %>"
                                    onclick="return confirm('Are you sure you want to delete this instrument?')"
                                    class="delete">🗑️ Delete</a>
                            </div>
                        </li>
                        <% }) %>
                </ul>
                <% } else { %>
                    <div class="card" style="text-align: center; padding: 40px;">
                        <p style="font-size: 1.2rem; color: var(--gold-dark);">No instruments in this category yet.</p>
                        <p style="margin-top: 15px;">
                            <a href="/instruments/new/<%= category.id %>" class="btn">Add Your First Instrument</a>
                        </p>
                    </div>
                    <% } %>
        </div>
    </body>

</html>