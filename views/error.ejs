<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Error - Musical Instruments Inventory</title>
        <link rel="icon" href="/favicon.svg" type="image/svg+xml">
        <link rel="stylesheet" href="/styles.css">
    </head>

    <body>
        <div class="container">
            <div class="card" style="max-width: 800px; margin: 60px auto; text-align: center; padding: 50px;">
                <div style="font-size: 5rem; margin-bottom: 20px;">⚠️</div>
                <h1 style="color: #ff4444; margin-bottom: 20px;">
                    <%= title || 'Error Occurred' %>
                </h1>

                <p style="font-size: 1.2rem; color: var(--gold-light); margin-bottom: 30px;">
                    <%= message || 'Something went wrong. Please try again.' %>
                </p>

                <% if (typeof details !=='undefined' && details) { %>
                    <div
                        style="background: var(--black-light); padding: 20px; border-radius: 8px; margin-bottom: 30px; border-left: 4px solid #ff4444;">
                        <p style="color: #ff6666; font-family: monospace; text-align: left;">
                            <%= details %>
                        </p>
                    </div>
                    <% } %>

                        <div class="form-buttons">
                            <a href="javascript:history.back()" class="btn"
                                style="background: var(--black-light); border: 2px solid var(--gold);">
                                ← Go Back
                            </a>
                            <a href="/" class="btn">
                                🏠 Home
                            </a>
                            <a href="/categories" class="btn">
                                📂 Categories
                            </a>
                        </div>
            </div>
        </div>
    </body>

</html>