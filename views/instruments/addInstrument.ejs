<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>
            <%= title %>
        </title>
        <link rel="icon" href="/favicon.svg" type="image/svg+xml">
        <link rel="stylesheet" href="/styles.css">
    </head>

    <body>
        <div class="container">
            <h1>Add New Instrument</h1>

            <% if (typeof errors !=='undefined' && errors.length> 0) { %>
                <div class="error">
                    <strong>Please fix the following errors:</strong>
                    <ul>
                        <% errors.forEach(error=> { %>
                            <li>
                                <%= error.msg %>
                            </li>
                            <% }); %>
                    </ul>
                </div>
                <% } %>

                    <form action="/instruments/new/<%= categoryId %>" method="post">
                        <label for="name">Instrument Name:</label>
                        <input type="text" id="name" name="name" value="<%= data.name || '' %>" required>

                        <label for="brand">Brand:</label>
                        <input type="text" id="brand" name="brand" value="<%= data.brand || '' %>" required>

                        <label for="price">Price ($):</label>
                        <input type="number" step="0.01" id="price" name="price" value="<%= data.price || '' %>"
                            required>

                        <label for="quantity">Quantity:</label>
                        <input type="number" id="quantity" name="quantity" value="<%= data.quantity || '' %>" required>

                        <div class="form-buttons">
                            <button type="submit">Add Instrument</button>
                            <a href="/categories/<%= categoryId %>">Cancel</a>
                        </div>
                    </form>
        </div>
    </body>

</html>